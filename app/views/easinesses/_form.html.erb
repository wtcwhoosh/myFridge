<%= form_for(@easiness) do |f| %>
  <% if @easiness.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@easiness.errors.count, "error") %> prohibited this easiness from being saved:</h2>

      <ul>
      <% @easiness.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
