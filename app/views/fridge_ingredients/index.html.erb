<h1>Listing fridge_ingredients</h1>

<table>
  <tr>
    <th>User</th>
    <th>Ingredient</th>
    <th>Quantity</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @fridge_ingredients.each do |fridge_ingredient| %>
  <tr>
    <td><%= fridge_ingredient.user_id %></td>
    <td><%= fridge_ingredient.ingredient_id %></td>
    <td><%= fridge_ingredient.quantity %></td>
    <td><%= link_to 'Show', fridge_ingredient %></td>
    <td><%= link_to 'Edit', edit_fridge_ingredient_path(fridge_ingredient) %></td>
    <td><%= link_to 'Destroy', fridge_ingredient, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Fridge ingredient', new_fridge_ingredient_path %>
