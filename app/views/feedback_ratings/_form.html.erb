<%= form_for(@feedback_rating) do |f| %>
  <% if @feedback_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feedback_rating.errors.count, "error") %> prohibited this feedback_rating from being saved:</h2>

      <ul>
      <% @feedback_rating.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
